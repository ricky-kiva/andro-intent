<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.IntentApp"
        tools:targetApi="31">
        <activity
            android:name=".MoveForResultActivity"
            android:exported="false" />
        <activity
            android:name=".MoveWObjActivity"
            android:exported="false" />
        <activity
            android:name=".MoveWDataActivity"
            android:exported="false" />
        <activity
            android:name=".MoveActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>

                <!-- this states 'MainActivity' is the 'entry point' of the app -->
                <action android:name="android.intent.action.MAIN" />

                <!-- this states 'MainActivity' will be available at 'home screen' as a 'launcher' -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>